--- !ruby/object:RI::MethodDescription 
aliases: 
- !ruby/object:RI::AliasName 
  name: match_regex
block_params: 
comment: 
- !ruby/struct:SM::Flow::P 
  body: Given a Regexp or String, passes if actual.match(pattern)
- !ruby/struct:SM::Flow::P 
  body: "@example"
- !ruby/struct:SM::Flow::VERB 
  body: "  email.should match(/^([^\\s]+)((?:[-a-z0-9]+\\.)+[a-z]{2,})$/i)\n  email.should match(&quot;@example.com&quot;)\n  zipcode.should match_regex(/\\A\\d{5}(-\\d{4})?\\z/)\n  zipcode.should match_regex(&quot;90210&quot;)\n"
- !ruby/struct:SM::Flow::P 
  body: "@note Due to Ruby's method dispatch mechanism, using the `#match` matcher within a custom matcher defined via the matcher DSL (`RSpec::Matcher.define`) will result Ruby calling the wrong `#match` method and raising an `ArgumentError`. Instead, use the aliased `#match_regex` method."
full_name: RSpec::Matchers#match
is_singleton: false
name: match
params: (expected)
visibility: public
